export const resetCss = {
    all: {
        padding:0,
        margin: 0,
        listStyle: "none",
        background:"#ccc"
    }
}


export const commonEvent = {
    watchUrl() {
        const url = location.href;
        console.log(url)
    }
}